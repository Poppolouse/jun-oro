import { GameDetail, GameResult, Query } from './types';
export declare function find(query: Query): Promise<{
    total: number;
    data: GameResult[];
}>;
export declare function get(id: number): Promise<GameDetail | null>;

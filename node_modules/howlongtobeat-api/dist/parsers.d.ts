import { GameDetail, GameResult, Query } from './types';
export declare function parseQuery(query?: Query): string;
export declare function parseGameResults(html: string): {
    total: number;
    data: GameResult[];
};
export declare function parseGameDetail(html: string): GameDetail;
